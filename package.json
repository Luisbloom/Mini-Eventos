{
  "name": "jartiland-amongus",
  "version": "1.0.0",
  "private": true,
  "description": "Portal multi-evento de la comunidad Jartiland",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "init-db": "node src/init-db.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "helmet": "^8.3.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
